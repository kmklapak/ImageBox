<%= javascript_include_tag "images/images" %>

<% if user_signed_in? %>
  Signed in as <%= current_user.username %>. Not you?
  <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
  <%= render 'image_pagination' %>
<% else %>

<% end %>

<%= render 'bootstrap_modal' %>